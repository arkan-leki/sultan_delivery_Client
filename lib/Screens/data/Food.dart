class Food {
  String id;
  String image;
  String price;
  String disprice;
  String title;
  String subtitle;
  String detiles;
  bool status;

  Food(this.id, this.image, this.price, this.disprice, this.title,
      this.subtitle, this.detiles, this.status);
}
